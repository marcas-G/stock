# I3 RED —— HEAD 版本 + throttle 持续 False 测试（pytest 挂死 → timeout 12 杀）
# cmd: cd platform && timeout 12 .venv/bin/python -m pytest -q tests/test_batch_flock.py -k 'throttle_closed'
exit=124（124 = timeout 杀进程：看门狗未收敛，挂死）
