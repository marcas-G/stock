# R01-DATA-I4 证据：CH delist_date 为 Date 类型崩溃

## 修复前（RED）

### 1. 原 probe（Nullable(Date) 列 → ComputeError）

命令：`platform/.venv/bin/python /tmp/opencode/reviewer-data/probe_delist_date_ch.py`
原始输出：`probe_delist_date_ch_before.txt`

```
polars.exceptions.ComputeError: could not append value: 2024-06-01 of type: date
to the builder; make sure that all rows have the same schema ...
```

### 2. 测试 fixture 改为生产类型（date? = CH Nullable(Date)）后 ch 腿全挂

命令：`cd platform && .venv/bin/python -m pytest -q tests/test_pit_universe.py`
输出摘要：`26 failed, 62 passed`；单条原始输出：`i4_before_failure.txt`

```
FAILED tests/test_pit_universe.py::test_delisting_boundary[ch]
E  polars.exceptions.ComputeError: could not append value: 2024-06-01 of type: date ...
```

## 修复后（GREEN）

```
$ cd platform && .venv/bin/python -m pytest -q tests/test_pit_universe.py
........................................................................ [ 80%]
..................                                                       [100%]
90 passed in 12.83s
```

原 probe after（命令同 before，输出：`probe_delist_date_ch_after.txt`）：

```
dtypes: {'date': Date, 'code': String, 'in_universe': Boolean, 'is_listed': Boolean,
         'list_days': Int64, 'is_st': Boolean, 'exchange': String}
┌────────────┬────────┬─────────────┬───────────┐
│ 2024-05-31 ┆ 000002 ┆ true        ┆ true      │
│ 2024-06-01 ┆ 000002 ┆ false       ┆ false     │   ← 退市边界 PIT 正确
└────────────┴────────┴─────────────┴───────────┘
```

## 改动 / 测试

- 生产：`platform/src/factorlab/adapters/read/universe.py` `_uf_skeleton_ch`
  delist 表达式统一 `toString(toYYYYMMDD(toDateOrNull(toString(col))))`
  （Date/String/NULL/空串全兼容；列缺失仍字面 NULL）。
- 测试设施：`platform/tests/dualbridge.py` 新增 `date?` kind（duckdb VARCHAR /
  CH Nullable(Date)）。
- 测试：`test_pit_universe.py` 主 fixture `delist_date` 由 `str?` → `date?`（全部
  PIT 测试锁 Date 行为）；`test_delist_date_string_column_compatible` 锁 String
  旧环境兼容。
