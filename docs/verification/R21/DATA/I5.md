# R01-DATA-I5 证据：rebuild 崩溃续跑重复行 + integrity gate

## 修复前（RED）

命令：`cd platform && .venv/bin/python -m pytest -q tests/test_rebuild.py -k "duplicate or crash"`

```
FAILED tests/test_rebuild.py::test_rebuild_resume_after_crash_no_duplicate_rows
  （重拉未记录日期 → daily 出现同 (trade_date, ts_code) 两行）
FAILED tests/test_rebuild.py::test_build_final_db_aborts_on_duplicate_daily
  E  Failed: DID NOT RAISE ValueError
2 failed, 1 passed
```

旧机制：`_rebuild_daily_table` 对非 stock_st 表 `dedup=False`；manifest 每 20 批
落盘 → 崩溃窗口内已 INSERT 未记录的日期续跑重插；`build_final_db` 无 integrity 门。

## 修复后（GREEN）

```
$ cd platform && .venv/bin/python -m pytest -q tests/test_rebuild.py tests/test_atomicio.py
................................                                         [100%]
32 passed in 22.51s
```

新增测试（`platform/tests/test_rebuild.py`）：
- `test_rebuild_resume_after_crash_no_duplicate_rows`：staging 已有未记录日期 →
  续跑 `dates_fetched == []`、无重复、manifest 对账补记。
- `test_rebuild_resume_false_replaces_existing_rows`：`resume=False` 先清表全量
  重拉，旧行不残留。
- `test_build_final_db_aborts_on_duplicate_daily`：staging 有重复 → ValueError，
  且不产出 final.duckdb。
- `test_build_final_db_ok_without_duplicates`：对照不误伤。

## 改动

- `platform/src/factorlab/adapters/rebuild.py`
  - `_rebuild_daily_table`：启动时用 staging `DISTINCT trade_date` 对账 → 已落
    日期视为 completed（`upsert_on` 按日事务原子、单写者），不重拉不重插。
  - `rebuild_all`：`resume=False` 时先 `DROP TABLE IF EXISTS` 目标表。
  - `build_final_db`：产出前 `staging.integrity_check()`，`daily.duplicate_rows`
    失败即拒绝建库（fail loudly）。
