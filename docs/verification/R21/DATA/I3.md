# R01-DATA-I3 证据：pit_qfq 全局 asof base（chunk 不变性）

## 修复前（RED）

### 1. 原 probe 复现 full≠chunk

命令：`platform/.venv/bin/python /tmp/opencode/reviewer-data/probe_pit_chunk.py`
原始输出：`probe_pit_chunk_before.txt`

```
full  : [6.666666666666666, 7.333333333333333, 8.0, 9.0]
chunks: [10.0, 11.0, 8.0, 9.0]
equal: False
```

### 2. 新测试（API 未实现）

`tests/test_adjust.py` 新增测试导入 `load_pit_qfq_base_adj`：

```
ImportError: cannot import name 'load_pit_qfq_base_adj' from
'factorlab.adapters.read.adjust'
```

## 修复后（GREEN）

### 1. 原 probe 的 adapter 级 after（全局 base 提供后 full == chunks）

命令：`platform/.venv/bin/python docs/verification/R21/DATA/probe_pit_chunk_after.py`
原始输出：`probe_pit_chunk_after.txt`

```
global pit base @asof: [{'code': '000001', '__factorlab_pit_qfq_base_adj': 1.5}]
full  : [6.666666666666666, 7.333333333333333, 8.0, 9.0]
chunks: [6.666666666666666, 7.333333333333333, 8.0, 9.0]
equal : True
```

### 2. 测试

```
$ cd platform && .venv/bin/python -m pytest -q tests/test_adjust.py
......................                                                   [100%]
22 passed in 1.25s
```

新增测试（`platform/tests/test_adjust.py`）：
- `test_view_pit_qfq_global_base_full_equals_chunked`（full vs chunked 逐值一致）
- `test_view_pit_qfq_global_base_differs_from_frame_latest`（全局 base ≠ 帧内 latest 时以全局为准）
- `test_view_pit_qfq_no_global_base_keeps_frame_fallback`（standalone 契约不破坏）
- `test_load_pit_qfq_base_adj_latest_per_code_at_asof`（双腿 loader：每 code <= asof 最新非 null）

## 改动

- `platform/src/factorlab/adapters/read/adjust.py`
  - `view_prices(..., pit_qfq_base_col=None)`：pit_qfq 用外部全局 base 列（factor = adj/base）
  - `load_pit_qfq_base_adj(rd, asof)`：复用 `load_qfq_base_adj` SQL（argMax(adj_factor, trade_date)
    WHERE trade_date <= asof），列名 `__factorlab_pit_qfq_base_adj`

## 未解决（接线待 coordinator）

- `app/run.py` 未改（边界要求）：需要在 `adjustment == "pit_qfq"` 时调用
  `load_pit_qfq_base_adj(rd, asof)`、join 到 panel、传 `pit_qfq_base_col`；精确片见总结。

## 接线验证（内存 patch run.py 副本，真实 run_factor）

命令：`platform/.venv/bin/python docs/verification/R21/DATA/verify_wiring_run.py`
原始输出：`verify_wiring_run.txt`

```
patched run.py loaded; all anchors matched
pit_qfq FULL/60/120 equal: True True                  ← 接线后
event-day 95 signal (expect raw 19.50, factor=1.0): 19.499999610907018
UNWIRED (current run.py) pit_qfq FULL/60 equal: False ← 当前 run.py 未接线仍分块依赖
prod 600005 GATE FIRED: ...                           ← C1 接线同时验证
```

验证方式：把总结里的接线片段文本替换到 run.py 的 `/tmp` 副本（真实文件未改），
用同一 fixture 真跑 FULL/CHUNK-60/120。
