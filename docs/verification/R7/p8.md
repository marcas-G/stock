# R7 收口 + P8 使命判定（单仓单树重构 + 数据接口收口）

日期：2026-09-15 · 分支 `restructure/monorepo` @ `6aa82fe` · 远端 `github.com/marcas-G/stock`

## 1. 远端状态（权威复核）

| 远端分支 | SHA | 角色 |
|---|---|---|
| **`restructure/monorepo`** | `6aa82fe` | **默认分支**（已切换；单仓单树） |
| `main` | `ad17f3b` | 旧平台分支（**保留 30 天**，历史备份） |
| `research` | `eec9990` | 旧研究分支（保留 30 天） |
| `workspace` | `a045d7d` | 旧工作区文档分支（保留 30 天） |

- 推送：`git push -u origin restructure/monorepo`（普通 push，**无 force**）；
- 默认分支切换：API PATCH `default_branch=restructure/monorepo` → HTTP 200；
- 旧三分支的删除排在 **2026-10-15 之后**（程序见 `docs/remote-cleanup-checklist.md` §4；届时先切默认分支已做，再删旧支、再把新历史推成 `main`——顺序硬约束，避免任何 force）。

## 2. P8-Q1 是否完成 Intended Mission？

**Intended Mission**（用户 2026-09-14/15 指令）：① 文件级重构，让结构"清晰有逻辑"；
② **数据接口整理**为重点；③ 同步修订文档。

**观察到的结果**：

| 诉求 | 结果 | 证据 |
|---|---|---|
| 单仓单树 | 仓库根 = `platform/` + `research/` + `docs/`（+ 本地 `data/`/`_archive/`/`projects/`） | R1/R2；纯移动门 754 文件 0 丢失 |
| 平台包分层收口 | 16 个顶层遗留项归位；**core→config 欠账结清**（RunContext 迁 app，core 禁 import config 进静态门）；config 叶化（无 import 副作用） | R2；架构门 10 passed / 0 skip |
| 因子库清晰 | 152 spec → **14 族**（`factor/<族>/<短名>.yaml`），档案同族同短名镜像；索引自动生成 + byte-equality 门 | R5；`docs/index/factors.md`（309 行） |
| **数据接口（重点）** | 读侧：研究侧 ≥5 份 tick 读 + 3 份 lob 读 + bars/daily 读 → **平台单点**（tick_read/lob_read/bars_read）+ lib 薄封装；写侧：四套 writer/flock/`_SUCCESS`/state → **lib.writekit 单点**（ch_ingest state 目录→JSON 迁移 + convert_minutes 收敛）；契约：列契约 3 套→1 套派生（DDL↔契约同源门）、`parse_ms` 3 入口 parity、板块分类单点、路径/分区字面量收敛 | R4 三批；**读侧对照 11/11 逐字节一致**（含 2,581 万行 orders 全日）；check-day **max\|Δ\|=0**；真数据 parse_ms/板块/SQL 三项逐字节对照 |
| 文档修订 | 5 类矛盾单点化；旧路径 **36→0**；验证流水归档（两战役）；三份树级 README/CLAUDE/AGENTS 重写为单树纪律 | R6；`docs/verification/README.md`（含旧→新映射） |

**结论：Mission satisfied**（判决依据 = 下方门全绿 + 每项诉求都有可复现证据）。

## 3. P8-Q2 原有问题是否真正改善？

| 原问题（P0） | Before | After |
|---|---|---|
| 目录对不上/不清晰 | 容器 + 两 worktree + 三份 docs（30 份 spec 逐字节重复） | 一棵树三棵树，契约文档单副本，index 可查 |
| 数据接口多套做法 | 读 4 方式、写 8 家族、标记 4 形态、列契约 3 套 | 读/写/契约各单点；标记 2 形态（`_SUCCESS` + state JSON）；常驻门 G-CONTRACT/G-MARK/G-READ（报告）+ G-INDEX/G-IMPORTS/G-COPY（强制） |
| 门会假失败/死门 | `lint` 对 params 模板假错；web 集成测试被自有 results 打红；`_env` 门在缺 worktree 时静默 skip | lint 与引擎同序（152/152）；web 门按具体产物判 skip；单树门**不再 skip** |
| 迁移遗漏无人发现 | 无导入解析门 | **G-IMPORTS**（全仓 296 文件 + 负向自检；实测抓到 RunContext 迁移漏点） |

## 4. P8-Q3 真实运行环境下是否成立？

- 平台全量 **2513 passed / 13 skipped / 0 failed**（新布局 `platform/` 下；基线 2486+13）；
- 研究侧 **203 passed / 2 skipped**（emb；T1 用例 importorskip 不假通过）+ T1 **35 passed**（平台 venv）；
- 真实链路：**check-day max\|Δ\|=0**（5249 行，平台引擎 × 本地 parquet 对拍）、**读侧对照 11/11**、金样 pins 14/14；
- **`data/` 零改动**（2026-09-15 当日改动条目数 = 0；df 基线 338G 可用）。

## 5. P8-Q4 是否存在"规格满足但使命失败"？

- **未竟项显式登记**（`docs/pending-items.md`）：#12 表名常量单点（460 处 SQL，风险>收益）、
  #12② 入口改名（阻塞于用户级技能副本）、#13 G-READ 转强制（需 AST 判据）、
  #14 平台侧原子写补齐 + `adapters/batch_flock.py`（P-5 真实现）——均带启动条件与风险说明，**不静默**。
- **研究者判断项**：5 组"同公式多假设"经实测**无真重复**（direction/params/process 差异），
  未按原计划归档——改为索引内成组展示并留研究者拍板（R5 证据）。
- **altitude 检查**：新树下"从任一切入点回答'它在哪、谁生产它、怎么跑'"——根 README（导览）、
  platform/research/docs 三份 README（各自入口）、`docs/index/factors.md`（因子）、
  `docs/data-map.md`（数据）、`docs/verification/README.md`（证据）均可一句话定位 ✓。

## Verdict

**PASS** —— 门全绿、每项诉求有可复现证据、未竟项显式登记、五条纪律（No Leap / No Orphan /
Single Primary Ownership / No Hidden Design / Evidence over Claim）遵守。
