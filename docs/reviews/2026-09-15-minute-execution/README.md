# 分钟级执行详细方案（2026-09-15）

**这是什么**：M8 执行层扩展——「日频信号 + 次日分钟窗口成交」的详细设计与实施计划。
**给谁**：开发团队按此落实；后续复查走 `docs/reviews/README.md` 的评审流程。
**状态**：设计已确认（用户问答定稿）；实施计划待执行（7 个任务，TDD，逐任务可独立验收）。
**关系**：与 `docs/reviews/2026-09-15-open-operators/`（开放算子）相互独立，可并行。

## 文件

| 文件 | 内容 |
|---|---|
| `design.md` | 设计规格：需求与决策 / 配置面（窗口/价格口径/分批/参与率/触发/兜底）/ 真实约束 / M8 接口 / A股细节 / 验收 |
| `plan.md` | 实施计划：7 个任务（配置域 → 纯窗口引擎 → 分钟读取 → 窗口成交 → run_backtest 集成 → 持久化 → CH 端到端），含精确文件路径与失败测试代码 |

## 关键决策

1. **日频信号 + 分钟执行**：信号仍 EOD 产出；成交挪到次日分钟窗口（默认 `[0,30]`，VWAP，参与率 10%）。
2. **全可配**：执行窗口、价格口径（vwap/open/close/twap/mid）、分批切片、参与率、**价格触发**（limit / vwap_offset）、兜底（none/close）。
3. **尽量模拟真实**：一字涨跌停不成交、停牌跳过、参与率上限 + 未成顺延、限价未触发不成交（默认）、成本叠加滑点。
4. **向后兼容**：默认 `NEXT_OPEN` 逐值不变；`NEXT_CLOSE` 的 7 处显式拒绝保持不动。
5. **V1 边界**：量能触发（V2）、分钟 NAV（V2）、盘中临停规则库（另立）；tick 不参与。

## 验收锚点

- `NEXT_OPEN` 回归零差异；配置非法全部 fail fast；
- 小样本手算对拍（VWAP/限价/参与率顺延/部分成交/兜底各一例）；
- 一字板/停牌/未触发不成交；持久化 round-trip；
- CH 端到端（20 只 × 1 个月）+ 全门绿（`make gates`，pytest ≥ 2570/13）。

## 执行注意

- 工作区存在 R21 并发改动：所有提交按路径精确 `git add`，只动计划列出的文件；
- 分钟数据仅 CH 后端；按决策日分块读取（16GB 内存纪律）；
- 分钟价格与执行/NAV 均为 raw（与现有 M8 一致）；信号 qfq 口径不变。
