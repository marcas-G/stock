# R37-AGG-I1 证据：门控成员 z 分爆尾（momentum_20d_turnrank_top2）

公式：`signal = ts_mean(close,20)/ts_delay(close,20)-1` × 换手 rank 前 2% 门控（98% 行被乘 0）。

2026-07-31 分布（`panel.parquet`）：n=5203；98% 值 = 常数 +0.0969；激活票范围 -11.28 ~ +1.22；
该日常数由标准化把 0 平移而来（零膨胀 → 截面 std 极小 → 激活票 z 爆尾）。

样本：600105.SH 当日 z=-11.28，权重 -1/9 → 贡献 +1.2537，占复合分 1.6379 的 **76.5%**；
若无此贡献（其余 8 员合计 0.384），该股排名约第 ~800/5199 → 落选 Top30。当天它被推为第 1 名。

9 员平均 |z|（全期）：momentum 0.206（最小）、其余 0.50~0.75 —— 平时几乎不出声（98% 挤常数），
**一旦激活（前 2%）单票即可决定聚合成败**。脉冲式成员对等权线性聚合的尾部鲁棒性构成实质影响。

建议（团队）：① composite 层给成员加可选 `clip`/`cs_rank` 变换（spec/params 级）；
② 或门控型成员 spec 的 process 改 cs_rank；③ 聚合口径文档明示"成员分布假设"。
