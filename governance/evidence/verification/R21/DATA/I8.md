# R01-DATA-I8 证据：refresh 丢弃失败原因

## 修复前（RED）

命令：`cd platform && .venv/bin/python -m pytest -q tests/test_refresh.py -k failed`

```
FAILED tests/test_refresh.py::test_refresh_records_failed_errors - KeyError:
  'failed_errors'
1 failed, 2 passed
```

`refresh.py:49-50` `except Exception: failed.add(d)` —— rebuild 的
`_rebuild_daily_table` 已记录 `failed_errors`，refresh 丢弃。

## 修复后（GREEN）

```
$ cd platform && .venv/bin/python -m pytest -q tests/test_source.py tests/test_refresh.py
..........................................................               [100%]
58 passed in 8.52s
```

新增测试 `tests/test_refresh.py::test_refresh_records_failed_errors`：
report 与 manifest 的 `failed_errors[date]` 均含原始异常文本（"拉取失败"）。

## 改动

- `platform/src/factorlab/adapters/refresh.py`：逐表 `errors` 字典，失败
  `str(exc)[:120]`，成功清除；写入 manifest `failed_errors` 与
  `report["tables"][table]["failed_errors"]`（与 rebuild 同语义）。

## 未解决

- `factorlab data update` 的终端摘要仍只打印失败日期列表（`main.py:353-369`
  只取 `info["failed"]`）；如需终端展示原因需改 CLI（本次未动）。
