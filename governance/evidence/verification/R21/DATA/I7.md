# R01-DATA-I7 证据：duckdb vs ch vol/amount 单位漂移

## 修复前（RED）

### 1. 新测试：duckdb 腿恒等映射（未转换）

命令：`cd platform && .venv/bin/python -m pytest -q tests/test_source.py -k normalizes`

```
FAILED tests/test_source.py::test_load_daily_normalizes_duckdb_units[duckdb]
FAILED tests/test_source.py::test_load_daily_fill_state_normalizes_duckdb_units[duckdb]
2 failed, 2 passed     （ch 腿恒等=已 canonical，按预期通过）
```

### 2. 机制级 before/after probe（同一 duckdb 源，恒等 vs 归一）

命令：`platform/.venv/bin/python docs/verification/R21/DATA/probe_unit_scale.py`
原始输出：`probe_unit_scale.txt`

```
identity (pre-fix): [{'volume': 1234.0, 'amount': 5678.0}]     ← 手/千元未转换
normalized (fix) : [{'volume': 123400.0, 'amount': 5678000.0}] ← 股/元 canonical
```

## 修复后（GREEN）

```
$ cd platform && .venv/bin/python -m pytest -q tests/test_source.py tests/test_refresh.py
..........................................................               [100%]
58 passed in 8.52s
```

契约变化顺带更新两条既有断言（原断言把 duckdb 腿 seed 当 canonical，正是"双腿测不出"
的盲区）：`test_source.py::test_load_daily_maps_volume_column`、
`test_run_factor.py::test_run_factor_macro_formula_column_refs_loaded` /
`test_run_factor_string_param_as_column` 改为按 backend 源单位 + canonical 期望。

## 改动

- `platform/src/factorlab/adapters/read/source.py`
  - `_DUCKDB_UNIT_SCALE = {"vol": 100.0, "amount": 1000.0}` + 单位契约注释
    （duckdb 源 = teajoin 原始 手/千元；ch 灌入 = 股/元）。
  - `_load_daily_duckdb` / `_fill_duckdb` 经 `_duckdb_daily_expr` / `_duckdb_fill_expr`
    归一（vol×100、amount×1000）。
- 新测试：`tests/test_source.py` 2 条双腿 canonical 断言（恒等实现会挂 duckdb 腿）。

## 假设 / 边界

- teajoin `amount` 原始单位=千元 属 tushare 约定（评审标注为推断）；`vol`=手 为
  tushare 约定。若某日 teajoin 改口径，需同步 `_DUCKDB_UNIT_SCALE` 并留证据。
