# R30 Task10 最小验收 spec：验证 CH 读路径能读到新增 tradeday（2026-09-15/16）。
# 非研究因子，仅验收用（存 evidence 不过库）。exclude_st: false → 无 stock_st 表时不 fail。
name: r30_accept_min
category: custom
direction: 1
universe:
  rules: {exclude_st: false, exchanges: ["SSE", "SZSE"]}
date:
  start: "2026-07-01"
  end: "2026-09-16"
process:
  - standardize()
formula: |
  signal = -close
