# Issues #34、#36、#38 处理与验证记录

更新：2026-09-25（UTC；Asia/Shanghai 为 2026-09-26）。本文汇总原始 nightly、后续修复和可复验结果；不代表 GitHub issue 已回填或关闭。

## #34：nightly G-TOPO 失败

原始日志：`/data/students/gaolei/quantresearch/results/platform/.nightly/20260922-030000.log`。

2026-09-22 nightly 的 `gates-offline` 因 G-TOPO 返回失败（rc=1），`gates-full` 通过 `make gates` 报失败（verify 记录 rc=2）。根因是 `research/tools/porteval/run.py` 执行 `import engine`，被解析到 `research/tools/lob_fact/core`，构成跨工具导入。该轮 platform、CH integration、platform tools、research tools、governance ops 测试及其余产品检查均通过。

修复提交 `dfbb59f`（2026-09-22 08:20 +0800）将 porteval 的 `engine.py` 改名为 `pv_engine.py`，并更新导入，消除模块名冲突。R43 deep verify 于 2026-09-25 复验：G-TOPO 为 0 处，`gates-offline`、`gates-full` 均 rc=0。完整记录见 [`tester/verify-deep-2026-09-25.log`](tester/verify-deep-2026-09-25.log)；原始失败见上述 nightly 日志。

**结论与范围**：nightly 中报告的 G-TOPO 缺陷已由提交修复并在后续全链验证通过；该结论只覆盖此项拓扑失败。

## #36：陈旧索引与未固定测试时钟

原始日志：`/data/students/gaolei/quantresearch/results/platform/.nightly/20260923-030002.log`。

本次失败分为两项：

1. `gates-full` 和 `product-index-check` 因产物区 `index/factors.md` 陈旧而失败；当时生成器发现 276 个因子，索引仍记录 269 个。该轮 `product-index-regen-check` 重生成索引后检查通过。research tools 的 `test_index_matches_generator` 是同一陈旧状态的测试表现，不是另一处生成器缺陷。
2. `test_freshness_gap_and_check` 未给 `check_freshness` 固定 `today`。2026-09-23 执行时，模拟日历只到 2026-09-21，代码转入 weekday-approx 口径并把滞后计为 3 天；测试却假定 2026-09-22 的 2 天结果。提交 `2907409`（2026-09-24 01:16 +0800）为测试传入固定 `today=2026-09-22`，消除时间依赖。

索引在后续被重生成；R43 deep verify 中索引检查和重生成后检查均 rc=0。当前定向复验命令为：

```text
governance/ops/heavy.sh platform/.venv/bin/python -m pytest -q \
  research/tools/factor_lib/tests/test_index.py::test_index_matches_generator \
  research/tools/xscore/tests/test_ref_sync.py::test_freshness_gap_and_check
```

结果：`2 passed in 0.41s`。R43 全链记录：[`tester/verify-deep-2026-09-25.log`](tester/verify-deep-2026-09-25.log)，其中 research tools `184 passed, 3 deselected`、`product-index-check rc=0`、`product-index-regen-check rc=0`，总结果 `rc=0`。

**结论与范围**：测试时钟问题由 `2907409` 修正；索引陈旧由产物索引重生成处理。当前定向检查及 R43 全链结果通过，但后续若研究产物新增或变化，仍需按索引生成流程重新生成并检查索引。

## #38：porteval 非标准 JSON 数值

原始问题记录为 R43 的 `R43-PORT-I1`：平收益/零方差时，信息比率和 Sharpe 比率未定义，旧实现产生 `NaN`/`Infinity`，默认 JSON writer 会输出非标准 JSON 常量，严格解析器拒绝。原 finding 位置见 `governance/evidence/reviews/findings.md` 的 `R43-PORT-I1`。

提交 `892da9a` 在 `pv_engine.py` 中将未定义比率表示为 `null`，并通过 `allow_nan=False` 的严格 JSON writer 阻止遗漏的非有限值进入产物；`run.py` 的 portfolio 与 manifest writer 也使用该 helper。

定向验证记录：

- [`issue-38-porteval-tests.log`](issue-38-porteval-tests.log)：porteval 测试 `12 passed in 0.14s`，包括零方差指标和 NaN/Infinity 严格序列化回归。
- [`issue-38-repro.txt`](issue-38-repro.txt)：平收益复现现得到 `ir=None`、`sharpe=None`、`vol=0.0`；严格 JSON 检查结果 `strict_json=ACCEPTED`。
- 可复验脚本：[`tester/repro_porteval_nonfinite_json.py`](tester/repro_porteval_nonfinite_json.py)。

R43 deep verify 于 2026-09-25、`892da9a` 之前运行，结果 `rc=0`（完整结果见 [`tester/verify-deep-2026-09-25.log`](tester/verify-deep-2026-09-25.log)）。该全链结果验证了 #34/#36 的修复状态；#38 另有提交后的 12 条定向测试和严格 JSON 复现。

**结论与范围**：#38 的修复已提交于 `892da9a`，定向测试和严格 JSON 复现通过；后续 deep/full 验证仍可作为补充回归，不影响本次修复结论。

提交后补验：`platform/.venv/bin/python -m pytest -q research/tools/porteval/tests platform/tools/ashare_ingest/tests/test_layout.py governance/ops/tests/test_check_tool_layering_regressions.py`，结果 **18 passed**，原始输出见 `issue-38-40-final.log`。
