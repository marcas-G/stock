"""factorlab 纯计算核（无 I/O、不读 settings、不碰文件系统）。

子模块：domain / numerics / qa / factor / spec / ops / engine / eval / strategy /
process(registry) / catalog_model / factio。依赖方向：core 不得 import ports/adapters/app/surfaces。
"""
