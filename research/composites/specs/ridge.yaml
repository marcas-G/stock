name: ridge
members: [cx_demo_x1, cx_demo_x2]
implementation:
  entrypoint: research.composites.implementations.ridge_pls_pca:ridge
params: {alpha: 1.0}
alignment: {join: intersection, missing_policy: reject}
output: {name: signal}
