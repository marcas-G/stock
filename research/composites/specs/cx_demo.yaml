name: cx_demo
members: [cx_demo_x1, cx_demo_x2]
implementation:
  entrypoint: research.composites.implementations.cx_demo:compute
params: {}
alignment: {join: intersection, missing_policy: reject}
output: {name: signal}
