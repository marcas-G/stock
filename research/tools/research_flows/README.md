# Research Flows 使用文档已迁移

正式的研究员使用手册已放到研究产物区：

```text
$QUANTRESEARCH_ROOT/knowledge/pipeline-usage.md
```

本文件只保留仓内指针。因子挖掘、xscore、策略执行和
`research-campaign` 的输入输出、YAML 示例、部署方式和锁箱规则都以研究产物区手册为准。

代码测试从该路径读取手册；`QUANTRESEARCH_ROOT` 未挂载时，手册示例测试会跳过。
