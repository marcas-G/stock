"""研究侧共享库（R4）：平台单点的薄封装 + 研究侧特有约定。

原则（R4 计划）：
- **实现只有一份**——数据读写一律经平台 `adapters.*`（tick_read / lob_read / …）；
- 研究侧只声明"投影"（列子集）与"缺数据返回 None"这类本地语义（批算按日跳过）；
- 任何本库模块都不得自己拼 `year=/month=` 或 `pl.scan_parquet` 读事实库（G-READ 门）。
"""
